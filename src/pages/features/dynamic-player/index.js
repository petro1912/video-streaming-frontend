import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";

const LiveStreams = () => {

    return (
        <Grid container spacing={6} sx={{mt: 5}}>
            <Typography variant="h4">Dynamic Player</Typography>
        </Grid>
    );

}

export default LiveStreams;